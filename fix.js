const fs = require('fs'); let c = fs.readFileSync('client_frontend/index.html', 'utf-8'); const start = c.indexOf('<div id=\"chatModal\"'); const end = c.indexOf('</div>\r\n    </div>', start) + '</div>\r\n    </div>'.length; if(start !== -1 && end > start) { c = c.slice(0, start) + c.slice(end); fs.writeFileSync('client_frontend/index.html', c); console.log('Removed chatModal'); } else { console.log('chatModal not found or bounds incorrect: ' + start + ', ' + end); }
